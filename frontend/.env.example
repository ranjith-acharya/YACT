# Backend URL for Vite dev server proxy (must match the port you use for `php artisan serve`).
# Default: http://localhost:8000. If you use e.g. --port=8002, set:
VITE_API_TARGET=http://localhost:8002
